# Bağlan Oto Care Database Configuration
# Copy this file to .env and update with your values

# Database Configuration
DB_NAME=baglan_oto_care
DB_USER=postgres
DB_PASSWORD=your_secure_password_here
DB_HOST=localhost
DB_PORT=5432

# Connection Pool Settings (if using with application)
DB_POOL_MIN=2
DB_POOL_MAX=10

# Backup Configuration
BACKUP_DIR=./backups
BACKUP_RETENTION_DAYS=30

# Application Settings (if needed)
APP_ENV=development
APP_DEBUG=true

# pgAdmin Configuration (for Docker)
PGADMIN_EMAIL=admin@baglanotocare.com
PGADMIN_PASSWORD=admin

# Notes:
# - Never commit .env file to version control
# - Use strong passwords in production
# - Update DB_HOST for remote databases
# - Adjust pool settings based on application needs
